import React from 'react';
import {StyleSheet, View, Text, SafeAreaView} from 'react-native';

const Home = () => {
  return (
    <SafeAreaView>
      <Text>Home</Text>
    </SafeAreaView>
  );
};

const styles = St;
export default Home;
